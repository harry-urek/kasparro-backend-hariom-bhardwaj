# =============================================================================
# Kasparro Backend - Environment Configuration Example
# =============================================================================
# Copy this file to .env and adjust values for your environment.
# For AWS deployment, configure these as App Runner environment variables.

# -----------------------------------------------------------------------------
# Environment Mode
# -----------------------------------------------------------------------------
# "dev"  - Enables Swagger/Redoc docs, debug logging, relaxed settings
# "prod" - Disables docs endpoints, stricter logging, production-optimized
ENV=dev

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
# PostgreSQL connection string (psycopg2 driver)
# Local:  postgresql+psycopg2://user:pass@localhost:5432/dbname
# Docker: postgresql+psycopg2://kasparro_user:kasparro_pass@db:5432/kasparro
# AWS:    postgresql+psycopg2://user:pass@rds-endpoint:5432/kasparro
DATABASE_URL=postgresql+psycopg2://kasparro_user:kasparro_pass@localhost:5432/kasparro

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# In prod mode with LOG_LEVEL=DEBUG, effective level is INFO (security)
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# ETL Configuration
# -----------------------------------------------------------------------------
# Enable/disable background ETL scheduler (true/false)
# Set to "false" for App Runner (use Lambda for scheduled ETL instead)
ETL_ENABLED=true

# Interval between ETL runs in seconds (default: 300 = 5 minutes)
ETL_INTERVAL_SECONDS=300

# -----------------------------------------------------------------------------
# External API Keys (Optional)
# -----------------------------------------------------------------------------
# CoinGecko API key (free tier works without key, pro tier requires it)
CGEKO_KEY=

# CoinPaprika API key (required for high-frequency requests)
COINPAPRIKA_API_KEY=

# -----------------------------------------------------------------------------
# Notifications (Optional)
# -----------------------------------------------------------------------------
# Slack webhook URL for critical error alerts
SLACK_WEBHOOK_URL=

# -----------------------------------------------------------------------------
# AWS-Specific (for Lambda ETL trigger)
# -----------------------------------------------------------------------------
# API URL for Lambda to call ETL endpoint (set in Lambda environment)
# API_URL=https://your-app.awsapprunner.com
